{
  "version": 2,
  "builds": [
    {
      "src": "api/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/mcp", "dest": "/api/mcp.ts" },
    { "src": "/health", "dest": "/api/health.ts" },
    { "src": "/", "dest": "/api/health.ts" }
  ]
}
